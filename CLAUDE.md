# CLAUDE.md

El contexto completo del proyecto está en **AGENTS.md** (lo usan tanto Claude Code como OpenCode).
Leelo antes de hacer cambios.

Recordatorios rápidos:
- MVP simple, sin dependencias ni build. Avisar si un pedido excede el MVP.
- Subir `VERSION` en `sw.js` después de cada cambio.
- Escapar todo texto del usuario con `Report.esc()`.
